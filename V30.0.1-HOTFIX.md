# MesHeures V30.0.1 — Hotfix

- Fixes the V30 reactive period-cache recursion (`calcPer` -> `MH28DataEngine.period`).
- `MH28DataEngine.period()` now calls the raw canonical calculator.
- Removes the legacy Intelligence DOM injector and its delayed duplicate-card injection.
- V30 owns navigation/render orchestration; V25 remains the canonical day/live renderer.
- Removes redundant startup render orchestration.
- Routes month mode and pay-period mutations through `mhRefresh()`.
- Keeps green reserved for primary/selected actions; secondary controls remain tonal.
- Version: 30.0.1 / versionCode 3001.
- Android signing workflow and persistent signing secrets are unchanged.
